{{- if .Values.deployNotebooks }}
{{- $datagen_image_stream := include "agent-mesh.imageStreamName" (dict "root" . "base" .Values.imageStreams.names.dataGeneration) }}
{{- $graphrag_image_stream := include "agent-mesh.imageStreamName" (dict "root" . "base" .Values.imageStreams.names.graphrag) }}
{{- $datagen_image := include "agent-mesh.imageStreamRef" (dict "root" . "base" .Values.imageStreams.names.dataGeneration "tag" .Values.dataGeneration.image.tag) }}
{{- $graphrag_image := include "agent-mesh.imageStreamRef" (dict "root" . "base" .Values.imageStreams.names.graphrag "tag" .Values.graphrag.image.tag) }}
---
apiVersion: kubeflow.org/v1
kind: Notebook
metadata:
  annotations:
    notebooks.opendatahub.io/inject-auth: "true"
    notebooks.opendatahub.io/last-image-selection: "{{ $datagen_image_stream }}:{{ .Values.dataGeneration.image.tag}}"
    opendatahub.io/image-display-name: {{ .Values.dataGeneration.image.title}}
    opendatahub.io/workbench-image-namespace: {{ .Values.imageStreams.namespace | quote }}
    openshift.io/description: ""
    openshift.io/display-name: {{ .Values.dataGeneration.notebook.name}}
    {{- if .Values.dataGeneration.notebook.mlflowInstance }}
    opendatahub.io/mlflow-instance: {{ .Values.dataGeneration.notebook.mlflowInstance }}
    {{- end }}
  name: {{ .Values.dataGeneration.notebook.name}}
  namespace: {{ .Values.namespace}}
  labels:
    app: {{ .Values.dataGeneration.notebook.name}}
    opendatahub.io/odh-managed: "true"
    opendatahub.io/dashboard: "true"
spec:
  template:
    spec:
      enableServiceLinks: false
      initContainers:
        - name: git-clone
          image: {{ $datagen_image }}
          command:
            - sh
            - -c
            - |
              REPO_NAME=$(basename "{{ .Values.repoUrl }}" .git)
              TARGET="/opt/app-root/src/$REPO_NAME"
              git config --global --add safe.directory "$TARGET"
              git config --global credential.helper '!f() { echo "username=${GIT_USERNAME}"; echo "password=${GIT_TOKEN}"; }; f'
              git init "$TARGET"
              git -C "$TARGET" remote add origin "{{ .Values.repoUrl }}" 2>/dev/null || true
              git -C "$TARGET" fetch origin "{{ .Values.repoRef }}"
              git -C "$TARGET" reset --hard FETCH_HEAD
          envFrom:
            - secretRef:
                name: code-understanding-env
          volumeMounts:
            - name: workspace-volume
              mountPath: /opt/app-root/src
      containers:
        - name: {{ .Values.dataGeneration.notebook.name}}
          image: {{ $datagen_image }}
          imagePullPolicy: Always
          workingDir: /opt/app-root/src
          ports:
            - containerPort: 8888
              name: notebook-port
              protocol: TCP
          resources:
            limits:
              cpu: "2"
              memory: 8Gi
            requests:
              cpu: "1"
              memory: 4Gi
          livenessProbe:
            httpGet:
              path: /notebook/{{ .Values.namespace }}/{{ .Values.dataGeneration.notebook.name }}/api
              port: notebook-port
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 1
            failureThreshold: 3
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: /notebook/{{ .Values.namespace }}/{{ .Values.dataGeneration.notebook.name }}/api
              port: notebook-port
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 1
            failureThreshold: 3
            successThreshold: 1
          volumeMounts:
            - name: workspace-volume
              mountPath: /opt/app-root/src
            - name: shm
              mountPath: /dev/shm
            - name: trusted-ca
              mountPath: /etc/pki/tls/custom-certs
              readOnly: true
            - name: runtime-images
              mountPath: /opt/app-root/pipeline-runtimes/
          envFrom:
            - secretRef:
                name: code-understanding-env
          env:
            - name: NOTEBOOK_ARGS
              value: >-
                --ServerApp.port=8888
                --ServerApp.token=''
                --ServerApp.password=''
                --ServerApp.base_url=/notebook/{{ .Values.namespace }}/{{ .Values.dataGeneration.notebook.name }}
                --ServerApp.quit_button=False
            - name: JUPYTER_IMAGE
              value: "{{ $datagen_image }}"
            - name: REQUESTS_CA_BUNDLE
              value: /etc/pki/tls/custom-certs/ca-bundle.crt
            - name: SSL_CERT_FILE
              value: /etc/pki/tls/custom-certs/ca-bundle.crt
            - name: PIPELINES_SSL_SA_CERTS
              value: /etc/pki/tls/custom-certs/ca-bundle.crt
            - name: KF_PIPELINES_SSL_SA_CERTS
              value: /etc/pki/tls/custom-certs/ca-bundle.crt
            - name: GIT_SSL_CAINFO
              value: /etc/pki/tls/custom-certs/ca-bundle.crt
            - name: PIP_CERT
              value: /etc/pki/tls/custom-certs/ca-bundle.crt
      volumes:
        - name: workspace-volume
          persistentVolumeClaim:
            claimName: datagen-storage
        - name: shm
          emptyDir:
            medium: Memory
        - name: trusted-ca
          configMap:
            name: workbench-trusted-ca-bundle
            optional: true
        - name: runtime-images
          configMap:
            name: pipeline-runtime-images
            optional: true
---
apiVersion: kubeflow.org/v1
kind: Notebook
metadata:
  annotations:
    notebooks.opendatahub.io/inject-auth: "true"
    notebooks.opendatahub.io/last-image-selection: "{{ $graphrag_image_stream }}:{{ .Values.graphrag.image.tag}}"
    opendatahub.io/image-display-name: {{ .Values.graphrag.image.title}}
    opendatahub.io/workbench-image-namespace: {{ .Values.imageStreams.namespace | quote }}
    openshift.io/description: ""
    openshift.io/display-name: {{ .Values.graphrag.notebook.name}}
    {{- if .Values.graphrag.notebook.mlflowInstance }}
    opendatahub.io/mlflow-instance: {{ .Values.graphrag.notebook.mlflowInstance }}
    {{- end }}
  name: {{ .Values.graphrag.notebook.name}}
  namespace: {{ .Values.namespace}}
  labels:
    app: {{ .Values.graphrag.notebook.name}}
    opendatahub.io/odh-managed: "true"
    opendatahub.io/dashboard: "true"
spec:
  template:
    spec:
      enableServiceLinks: false
      initContainers:
        - name: git-clone
          image: {{ $graphrag_image }}
          command:
            - sh
            - -c
            - |
              REPO_NAME=$(basename "{{ .Values.repoUrl }}" .git)
              TARGET="/opt/app-root/src/$REPO_NAME"
              git config --global --add safe.directory "$TARGET"
              git config --global credential.helper '!f() { echo "username=${GIT_USERNAME}"; echo "password=${GIT_TOKEN}"; }; f'
              git init "$TARGET"
              git -C "$TARGET" remote add origin "{{ .Values.repoUrl }}" 2>/dev/null || true
              git -C "$TARGET" fetch origin "{{ .Values.repoRef }}"
              git -C "$TARGET" reset --hard FETCH_HEAD
          envFrom:
            - secretRef:
                name: code-understanding-env
          volumeMounts:
            - name: workspace-volume
              mountPath: /opt/app-root/src
      containers:
        - name: {{ .Values.graphrag.notebook.name}}
          image: {{ $graphrag_image }}
          imagePullPolicy: Always
          workingDir: /opt/app-root/src
          ports:
            - containerPort: 8888
              name: notebook-port
              protocol: TCP
          resources:
            limits:
              cpu: "2"
              memory: 8Gi
            requests:
              cpu: "1"
              memory: 4Gi
          livenessProbe:
            httpGet:
              path: /notebook/{{ .Values.namespace }}/{{ .Values.graphrag.notebook.name }}/api
              port: notebook-port
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 1
            failureThreshold: 3
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: /notebook/{{ .Values.namespace }}/{{ .Values.graphrag.notebook.name }}/api
              port: notebook-port
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 1
            failureThreshold: 3
            successThreshold: 1
          volumeMounts:
            - name: workspace-volume
              mountPath: /opt/app-root/src
            - name: shm
              mountPath: /dev/shm
            - name: trusted-ca
              mountPath: /etc/pki/tls/custom-certs
              readOnly: true
            - name: runtime-images
              mountPath: /opt/app-root/pipeline-runtimes/
          envFrom:
            - secretRef:
                name: code-understanding-env
          env:
            - name: NOTEBOOK_ARGS
              value: >-
                --ServerApp.port=8888
                --ServerApp.token=''
                --ServerApp.password=''
                --ServerApp.base_url=/notebook/{{ .Values.namespace }}/{{ .Values.graphrag.notebook.name }}
                --ServerApp.quit_button=False
            - name: JUPYTER_IMAGE
              value: "{{ $graphrag_image }}"
            - name: REQUESTS_CA_BUNDLE
              value: /etc/pki/tls/custom-certs/ca-bundle.crt
            - name: SSL_CERT_FILE
              value: /etc/pki/tls/custom-certs/ca-bundle.crt
            - name: PIPELINES_SSL_SA_CERTS
              value: /etc/pki/tls/custom-certs/ca-bundle.crt
            - name: KF_PIPELINES_SSL_SA_CERTS
              value: /etc/pki/tls/custom-certs/ca-bundle.crt
            - name: GIT_SSL_CAINFO
              value: /etc/pki/tls/custom-certs/ca-bundle.crt
            - name: PIP_CERT
              value: /etc/pki/tls/custom-certs/ca-bundle.crt
      volumes:
        - name: workspace-volume
          persistentVolumeClaim:
            claimName: graphrag-storage
        - name: shm
          emptyDir:
            medium: Memory
        - name: trusted-ca
          configMap:
            name: workbench-trusted-ca-bundle
            optional: true
        - name: runtime-images
          configMap:
            name: pipeline-runtime-images
            optional: true
{{- end }}
